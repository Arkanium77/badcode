package ru.liga.intership.badcode.service;

//@RunWith(SpringRunner.class)
//@SpringBootTest
//@JdbcTest
public class PersonDatabaseExtractorTest {
    /*
    static DatabaseExtractor extractor;

    @BeforeClass
    public static void beforeClass() {
        extractor = new PersonDatabaseExtractor("jdbc:hsqldb:mem:test", "sa", "");
    }

    @Test
    public void extractAllPersonsReturn5SizeList() {
        String query = "SELECT * FROM person";
        Assertions.assertThat(extractor.extractByQuery(query).size()).isEqualTo(5);
    }

    @Test
    public void extractAdultPersonsReturn4SizeList() {
        String query = "SELECT * FROM person WHERE age>=18";
        Assertions.assertThat(extractor.extractByQuery(query).size()).isEqualTo(4);
    }

    @Test
    public void extractNonAdultPersonsReturn1SizeList() {
        String query = "SELECT * FROM person WHERE age<18";
        Assertions.assertThat(extractor.extractByQuery(query).size()).isEqualTo(1);
    }

 */
}